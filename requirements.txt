# Core Dependencies
PyPDF2>=3.0.0
Pillow>=10.0.0
pandas>=2.0.0
reportlab>=4.0.0
hachoir>=3.2.0
pdf2image>=1.16.0
pytest>=8.3.0
matplotlib>=3.7.0
rich>=13.7.0
click>=8.1.0
typer>=0.12.3
pyyaml>=6.0.0

# Optional Dependencies for Enhanced Metadata Writing

# For image EXIF metadata (JPEG, TIFF)
piexif>=1.1.3

# For audio metadata (MP3, FLAC, M4A, etc.)
mutagen>=1.47.0

# For Microsoft Office files (.docx, .xlsx, .pptx)
python-docx>=1.1.0
openpyxl>=3.1.0

# Installation Commands:
# Install core dependencies:
# pip install PyPDF2 Pillow pandas hachoir rich typer

# Install all optional dependencies:
# pip install piexif mutagen python-docx openpyxl

# Or install everything at once:
# pip install -r requirements.txt
